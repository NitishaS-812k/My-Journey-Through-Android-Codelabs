<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="scorebutton" parent="Widget.AppCompat.Button">
        >
        <item name="android:background">@drawable/button_background</item>
    </style>

    <style name="plus_button" parent="scorebutton">
        <item name="android:src">@drawable/ic_add</item>
        <item name="android:contentDescription">@string/add_button</item>
    </style>

    <style name="minus_button" parent="scorebutton">
        <item name="android:src">@drawable/ic_minus</item>
        <item name="android:contentDescription">@string/minus_button</item>
    </style>

    <style name="ScoreText">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Large</item>
        <item name="android:textStyle">italic</item>
    </style>

    <style name="TeamText">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Display1
        </item>
    </style>

</resources>
